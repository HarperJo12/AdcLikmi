<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoginTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:colorBackground">@color/colorPrimary</item>
        <item name="android:textColorSecondary">@color/colorTextLogin</item>
        <item name="android:textColor">@color/colorTextLogin</item>
        <item name="android:textColorHint">@color/colorTextHint</item>
        <item name="android:editTextColor">@color/colorTextLogin</item>
        <item name="colorAccent">@color/colorTextLogin</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="StyledButton" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/colorTextButton</item>
        <item name="android:textColorSecondary">@color/colorTextButton</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="colorButtonNormal">@color/colorButtonNormal</item>
    </style>

    <style name="StyledCardView" parent="Widget.MaterialComponents.CardView">
        <item name="android:layout_marginTop">@dimen/_3sdp</item>
        <item name="android:layout_marginLeft">@dimen/_3sdp</item>
        <item name="android:layout_marginRight">@dimen/_3sdp</item>
        <item name="android:layout_marginBottom">@dimen/_3sdp</item>
        <item name="cardCornerRadius">@dimen/_8sdp</item>
        <item name="cardElevation">@dimen/_8sdp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
